// Every selector starts with .openshowcase-player, so these styles never reach the host page.
export const css = `
.openshowcase-player .frame--browser {
  border-radius: 12px;
  box-shadow:
    0 0 0 1px rgb(15 23 42 / 0.06),
    0 2px 6px rgb(15 23 42 / 0.06),
    0 24px 60px -16px rgb(15 23 42 / 0.4);
}
`;
